import React from 'react';
import Header from '../home/components/Header';

const Basket = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Basket;
